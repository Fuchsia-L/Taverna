# Taverna — Codex 约束清单（修订版）

> 用途：每次让 Codex 改代码时作为约束上下文。  
> 目标：减少竞态、状态不一致、流式回归，不增加不必要耦合。

---

## 0. 分级规则

### MUST（必须）
违反会直接导致功能错误、数据错乱或线上风险。

### SHOULD（应该）
强烈建议，提升稳健性和可维护性。

### NICE（可选）
有收益但不强制。

---

## 1. 总体工程原则

### MUST
- 不只实现 happy path：每个功能覆盖正常、异常、边界（空数据/重复点击/并发请求）。
- 前后端状态变更必须有明确同步路径（SSE / API response / polling），不能靠“默认会同步”。
- 改动接口契约（字段、返回结构、事件类型）时，必须同步列出受影响调用方。
- 不写死业务常量；可配置项放配置层，流程逻辑放领域层，避免高耦合。

### SHOULD
- 每次改完列出至少 3 个失败场景及处理方式。
- 新增流程优先“增量扩展”，避免大面积重构已有链路。

---

## 2. 后端（FastAPI + SQLAlchemy + LLM）

### 2.1 API 与异常

#### MUST
- 业务端点要么本地捕获并返回可理解错误，要么交由统一异常处理器，不能静默失败。
- 所有流式端点出错时必须发 `error` SSE 事件；不能直接断流无信号。
- 事务边界必须明确（`session.begin()` 或等价可回滚方式），写操作失败不可部分提交。

#### SHOULD
- 非流式 fallback 前记录 `logger.exception(...)`，保留排障信息。

---

### 2.2 Tool Call 生命周期

#### MUST
- `assess/quiz`：交互式工具，必须进入 pending（保存 messages snapshot）并返回 `tool_input_required`。
- `plan/advance`：自动工具，立即执行，不等待用户输入。
- tool loop 必须有上限（如 5 轮），到上限优雅退出并通知前端。
- 恢复 pending 时校验关键数据：`conversation_id`、`tool_call_id`、snapshot 完整性。

#### SHOULD
- 工具参数解析做兼容（多层 JSON / 分片拼接），解析失败返回明确错误，不进入脏状态。

---

### 2.3 PendingToolCall 状态管理

#### MUST
- 同一 conversation 同时最多一个 pending。
- 新 pending 写入前清理旧 pending（或原子覆盖）。
- pending 被有效消费后必须删除，避免复用。

#### SHOULD
- 存储策略以 DB 为主，内存仅降级兜底（如迁移未完成）。
- 多实例场景避免“内存优先”读策略。

---

### 2.4 消息持久化与压缩

#### MUST
- 流式允许先推 token，但在 `done/paused/abort` 路径必须保证可恢复持久化。
- 压缩触发和推进逻辑必须可重复执行且幂等，避免重复压缩/漏压缩。
- `force_compress` 标志在所有工具路径（含 stream）都要完整透传。

#### SHOULD
- 持久化 assistant 时统一合并 `thinking_text`（若未带 `<think>`），避免刷新后思维丢失。

---

### 2.5 Retry / Rewind / Tool-response 并发安全

#### MUST
- Retry/Rewind/Tool-response 必须在 conversation lock 下执行关键状态变更。
- Retry/Rewind 前清理无效 pending，避免前端残留旧卡片。
- 对消息删除/回退后，关联调试信息（debug turns）要一致处理，避免索引错位。

#### SHOULD
- 避免“先释放锁再重取锁”的竞态窗口；无法避免时要有状态二次校验。

---

## 3. 前端（React + TypeScript）

### 3.1 SSE 事件处理

#### MUST
- 完整处理：`token/thinking/tool/tool_input_required/raw/done/error`。
- `token` 增量拼接，不可覆盖。
- 必须处理 `done` 和 `error`，确保 loading 正确结束。
- 收到 `tool_input_required` 必须渲染并绑定可提交卡片。

#### SHOULD
- 网络断开时给用户明确提示；可选重连策略。

---

### 3.2 消息列表与状态机

#### MUST
- 发送、retry、rewind、tool-response 后都要落到一致消息状态。
- Retry 目标必须是“最后一条真实 assistant 消息”，不能命中工具卡片占位消息。
- Rewind 要正确截断后再接收新回复，避免版本串线。
- Assess/Quiz 卡片状态单向：待输入 -> 已提交 -> 已完成/失效。

#### SHOULD
- 提交按钮防重复（禁用+请求中状态）；失败时可恢复并提示。

---

### 3.3 类型与契约同步

#### MUST
- 新增/修改 API 字段或 SSE 事件时，同时更新：
  - 后端 Pydantic/响应模型
  - 前端 `chatApi.ts` 解析
  - 前端 TS 类型（`types/chat.ts`）
- 不允许后端先改、前端靠 `any` 暂时兜底。

---

## 4. 前后端协同检查表（每次改动后）

- [ ] 本次后端状态变更，前端通过什么路径同步（SSE/API/poll）？
- [ ] 失败路径是否保持前后端一致（无“前端成功、后端失败”）？
- [ ] 是否存在并发竞态（重复点击、请求乱序、双开页面）？
- [ ] 是否涉及 pending 生命周期（set/get/consume/delete）完整闭环？
- [ ] 是否涉及流式结束补偿保存（done/paused/abort）？
- [ ] 是否改了契约字段/事件类型并完成双端同步？
- [ ] 是否需要 migration，及无表时降级行为是否可用？
- [ ] 是否列出至少 3 个失败场景与处理结果？

---

## 5. Codex 输出格式要求（给 AI 的附加约束）

### MUST
- 先给“改动摘要”，再给“受影响文件”，再给“风险与验证”。
- 若改了函数签名/返回结构，必须列出所有调用点。
- 若无法完成某项验证（测试/编译/运行），必须明确说明。

### SHOULD
- 优先给低耦合改法：抽辅助函数、保持接口稳定、减少跨层依赖。